# integrations

Reserved for external integration boundaries: n8n webhook emitters (Phase 6)
and the CINC service (Phase 7). Per CLAUDE.md §20/§22, core scheduling logic
must not depend on either directly — keep both isolated here behind a small
interface.
